using UnityEngine;
using System.Collections;

public class SpawnEnabled : MonoBehaviour {
	public bool enabled;
	// Use this for initialization
	void Start () {
		enabled = true;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
